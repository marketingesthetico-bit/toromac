import ArticleDetail from '../../components/marketing/ArticleDetail';

// El componente compartido lee useParams().slug y resuelve el articulo
// segun el idioma activo (useLang).
export default function ArticuloDetalleEs() {
  return <ArticleDetail />;
}
